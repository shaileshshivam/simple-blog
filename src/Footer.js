import { Typography } from "@material-ui/core";

const Footer = (props) => {
  return (
    <footer className="footer">
      <Typography variant="button">Valar Morghulis</Typography>
    </footer>
  );
};

export default Footer;
